/*
 * logic_func.c
 *
 *  Created on: 19 нояб. 2020 г.
 *      Author: MMorozov
 */

#include "main.h"

uint8_t LOGIC_function(uint8_t DINn_rising, uint8_t DINn_faling)
{
	if((!DINn_rising) && (DINn_faling))
	{

	}
	else if((DINn_rising) && (!DINn_faling))
	{

	}

	return 0;
}
